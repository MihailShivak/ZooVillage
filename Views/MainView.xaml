<UserControl x:Class="ZooVillage.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="ZOO VILLAGE" 
                   FontSize="80" 
                   FontWeight="ExtraBold" 
                   Foreground="#F1C40F" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Top" 
                        Margin="0,80,0,0">

                <Button Content="ИГРАТЬ" Width="280" Height="65" FontSize="24" FontWeight="Bold" 
                        Background="#27AE60" Foreground="White" BorderThickness="0" Cursor="Hand"
                        Margin="0,0,0,25" Click="PlayButton_Click"/>

                <Button Content="О ПРОЕКТЕ" Width="280" Height="65" FontSize="24" FontWeight="Bold" 
                        Background="#2980B9" Foreground="White" BorderThickness="0" Cursor="Hand"
                        Margin="0,0,0,25" Click="AboutButton_Click"/>

                <Button Content="ВЫЙТИ" Width="280" Height="65" FontSize="24" FontWeight="Bold" 
                        Background="#C0392B" Foreground="White" BorderThickness="0" Cursor="Hand"
                        Click="ExitButton_Click"/>
            </StackPanel>

            <Border Grid.Column="1" 
                    Background="#34495E" 
                    CornerRadius="20" 
                    BorderBrush="#F1C40F" 
                    BorderThickness="1.5" 
                    Margin="40, 40, 80, 40" 
                    VerticalAlignment="Bottom" 
                    Height="350">

                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="📢 НОВОСТИ И ИНФО" 
                               Foreground="#F1C40F" FontSize="24" FontWeight="Bold" 
                               HorizontalAlignment="Center" Margin="0,0,0,15"/>
                    <TextBlock Text="Здесь будет информация о проекте, патчах и достижениях." 
                               Foreground="#ECF0F1" FontSize="18" 
                               HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"
                               Width="300"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>